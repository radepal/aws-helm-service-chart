apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "auth-sidecar.fullname" . }}-test-connection"
  labels:
    {{- include "auth-sidecar.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "auth-sidecar.fullname" . }}:{{ .Values.auth_service.service.port }}']
  restartPolicy: Never
