---
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "service.name" . }}-secret
  namespace: {{ .Release.Namespace }}
data:
  {{- if .Values.project.includeAwsCredentials }}
  aws.accesskey: {{ .Values.secret.aws_accesskey | b64enc }}
  aws.secretkey: {{ .Values.secret.aws_secretkey | b64enc }}
  {{- end }}
type: Opaque

{{- if .Values.ingress.int.enabled }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.tls.cert.int.secret.name }}
  namespace: {{ .Release.Namespace }}
data:
  tls.crt: {{ .Values.tls.cert.int.secret.crt | b64enc}}
  tls.key: {{ .Values.tls.cert.int.secret.key | b64enc }}
type: kubernetes.io/tls
{{- end }}

{{- if .Values.oauth2.enabled }}
---
apiVersion: v1
kind: Secret
metadata:
  name: auth-sidecar
type: Opaque
data:
  {{- range $key, $value := .Values.oauth2.secret }}
  {{ $key }}: {{ $value | b64enc }}
  {{- end }}
{{- end }}


---
{{- if .Values.additionalparameters.secretsApplied }}
apiVersion: v1
kind: Secret
metadata:
  name: additional-secrets
type: Opaque
data:
  {{- range $key, $value := .Values.additionalparameters.secrets }}
  {{ $key }}: {{ $value | b64enc }}
  {{- end }}
{{- end }}
