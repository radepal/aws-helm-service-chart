Successfully installed {{ .Chart.Name }}.

The release of this service is named {{ .Release.Name }}.

To learn more about the release, try:

  $ helm status {{ .Release.Name }}
  $ helm get {{ .Release.Name }}

Use curl to test your service endpoint:

  export URL=$(kubectl get ingress {{ include "service.name" . }}-ing -o jsonpath='{.spec.rules[0].host}' -n {{ .Release.Namespace }})
  eval $(echo "curl -k https://$URL/api/v1/status")
